<%-include('base_views/header')-%>

<% userlist.forEach(user => { %>

    <tr>
        <td> <%= user.login %></td>
        <td> <%= user.password %></td>
    </tr>

<% }) %> 

<%-include('base_views/footer')-%>